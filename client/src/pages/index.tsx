import React, {  } from 'react';
import DotsBackground from './components/background';

export default function () {

    return (
        <section>
            <DotsBackground/>
        </section>
    );
}